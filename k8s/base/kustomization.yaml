apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: joy
resources:
- namespace.yaml
- configmap.yaml
# secret.example.yaml is intentionally NOT applied: create joy-secrets
# yourself (see that file) before applying.
- api.yaml
- workers.yaml
- backing.yaml
- hpa.yaml
- ingress.yaml
- jaeger.yaml
- minio-init-job.yaml
